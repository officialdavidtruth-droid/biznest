import { prisma } from "@/lib/prisma";
import { GrandeurContact } from "@/components/storefront/grandeur-restaurant";
import { getGrandeurRestaurantData } from "@/lib/grandeur-restaurant";
import { ThelusoHotel } from "@/components/storefront/theluso-hotel";
import { getHotelContent } from "@/lib/hotel-content";
import { renderUniversalSectionPage } from "@/components/storefront/universal-section-page-route";
export default async function ContactPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await prisma.store.findUnique({where:{slug},select:{businessType:true,template:{select:{name:true}}}});if(String(s?.businessType||'').toLowerCase().includes('hotel')||String(s?.template?.name||'').includes('THELUSO')){const raw=await prisma.store.findUniqueOrThrow({where:{slug},select:{id:true,slug:true,name:true,logoUrl:true,bannerUrl:true,storyImage:true,contactPhone:true,contactEmail:true,business:{select:{city:true,state:true,phone:true,email:true}}}});const hotelStore={...raw,address:[raw.business?.city,raw.business?.state].filter(Boolean).join(", ")||null,phone:raw.contactPhone??raw.business?.phone??null,email:raw.contactEmail??raw.business?.email??null};return <ThelusoHotel store={hotelStore} slug={slug} content={await getHotelContent(slug)}/>;}const data=await getGrandeurRestaurantData(slug);if(data)return <GrandeurContact store={data.store} slug={slug} items={data.items}/>;return renderUniversalSectionPage(slug,"contact");}