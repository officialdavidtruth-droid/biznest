import { isStorefrontRenderer } from "@/lib/storefront-routing"; import {notFound} from "next/navigation"; import {prisma} from "@/lib/prisma"; import {TasteHouseCategories} from "@/components/storefront/tastehouse"; import {ExampleStorefront} from "@/components/storefront/example-store"; import {EXAMPLE_TEMPLATE_NAME} from "@/lib/example-content";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const s=await prisma.store.findUnique({where:{slug},include:{template:true,business:true,products:{where:{isPublished:true},take:100,include:{category:true}},services:{where:{isPublished:true},take:100,include:{category:true}}}}); if(!s||s.status!=="ACTIVE")notFound(); const items=[...s.products.map(p=>({id:p.id,kind:"product" as const,name:p.name,description:p.description,price:Number(p.price),currency:p.currency,image:p.images[0]??null,categoryName:p.category?.name??null,type:p.type,rentalUnit:p.rentalPeriodUnit,isBookable:false})),...s.services.map(x=>({id:x.id,kind:"service" as const,name:x.name,description:x.description,price:Number(x.price),currency:x.currency,image:x.images[0]??null,categoryName:x.category?.name??null,type:"SERVICE",rentalUnit:null,isBookable:x.isBookable}))]; if(s.template?.name===EXAMPLE_TEMPLATE_NAME)return <ExampleStorefront store={{...s,sellsProducts:s.business?.sellsProducts??true}} slug={slug} items={items} mode="categories"/>; if(isStorefrontRenderer({ templateName:s.template?.name }, "tastehouse"))return <TasteHouseCategories store={{...s,sellsProducts:s.business?.sellsProducts??true}} slug={slug} items={items}/>; notFound();}
