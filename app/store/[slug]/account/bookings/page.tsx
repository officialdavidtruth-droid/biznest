import { notFound } from "next/navigation";
import { getStoreBranding } from "@/lib/actions/store-branding";
import { listStoreBookings } from "@/lib/actions/account";
export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const {slug}=await params; const store=await getStoreBranding(slug); if(!store) notFound(); const bookings=await listStoreBookings(slug); return <div><h1 className="text-xl font-bold text-slate-900">My Appointments</h1><p className="mt-1 text-sm text-slate-500">Bookings with {store.name}.</p><div className="mt-5 space-y-3">{bookings.map(b=><div key={b.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-center justify-between gap-3"><p className="font-semibold text-slate-900">{b.service.name}</p><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold">{b.status}</span></div><p className="mt-2 text-sm text-slate-500">{new Date(b.scheduledAt).toLocaleString()}</p></div>)}{bookings.length===0&&<div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-sm text-slate-500">No appointments with this business yet.</div>}</div></div>; }
