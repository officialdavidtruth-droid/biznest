import { notFound } from "next/navigation";
import { getStoreBranding } from "@/lib/actions/store-branding";
import { listStoreWishlist } from "@/lib/actions/account";
export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const {slug}=await params; const store=await getStoreBranding(slug); if(!store) notFound(); const items=await listStoreWishlist(slug); return <div><h1 className="text-xl font-bold text-slate-900">My Wishlist</h1><p className="mt-1 text-sm text-slate-500">Saved only from {store.name}.</p><div className="mt-5 grid gap-4 sm:grid-cols-2">{items.map(i=>{const x=i.product||i.service; return <div key={i.id} className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="aspect-[4/3] bg-slate-100">{x?.images?.[0]&&<img src={x.images[0]} alt="" className="h-full w-full object-cover"/>}</div><div className="p-4"><p className="font-semibold text-slate-900">{x?.name}</p></div></div>})}{items.length===0&&<div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-sm text-slate-500">Your wishlist is empty.</div>}</div></div>; }
