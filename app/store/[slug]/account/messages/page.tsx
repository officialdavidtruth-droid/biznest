import { notFound } from "next/navigation";
import { getStoreBranding } from "@/lib/actions/store-branding";
import { listStoreConversations } from "@/lib/actions/account";
export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const {slug}=await params; const store=await getStoreBranding(slug); if(!store) notFound(); const conversations=await listStoreConversations(slug); return <div><h1 className="text-xl font-bold text-slate-900">Support</h1><p className="mt-1 text-sm text-slate-500">Messages with {store.name}.</p><div className="mt-5 space-y-3">{conversations.map(c=><div key={c.id} className="rounded-2xl border border-slate-200 bg-white p-5"><p className="font-semibold text-slate-900">{c.messages[0]?.content ? "Conversation" : "Support conversation"}</p><p className="mt-1 text-sm text-slate-500">{c.messages[0]?.content ?? "No messages yet"}</p></div>)}{conversations.length===0&&<div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-sm text-slate-500">No support conversations yet.</div>}</div></div>; }
