import Link from"next/link";import{listCreativeProjects}from"@/lib/actions/creative-projects";export default async function ProjectsPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const projects=await listCreativeProjects(slug);return <div><div className="mb-6"><h1 className="text-2xl font-semibold">Projects</h1><p className="mt-1 text-sm text-muted-foreground">Manage briefs, quotes, design approvals and production.</p></div><div className="overflow-hidden rounded-xl border"><div className="grid grid-cols-[1fr_1.2fr_1fr_1fr] gap-3 border-b bg-muted/30 px-4 py-3 text-xs font-semibold uppercase tracking-wide text-muted-foreground"><span>Project</span><span>Client</span><span>Service</span><span>Status</span></div>{projects.length===0?<div className="p-8 text-center text-sm text-muted-foreground">No project requests yet.</div>:projects.map(p=><Link key={p.id} href={`/${slug}/admin/projects/${p.id}`} className="grid grid-cols-[1fr_1.2fr_1fr_1fr] gap-3 border-b px-4 py-4 text-sm hover:bg-muted/20"><span className="font-semibold">{p.projectNo}</span><span>{p.customerName}</span><span>{p.serviceType}</span><span className="text-xs font-semibold">{p.status.replaceAll("_"," ")}</span></Link>)}</div></div>}
