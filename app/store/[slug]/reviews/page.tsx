import {notFound} from "next/navigation"; import {prisma} from "@/lib/prisma"; import {TasteHouseReviews} from "@/components/storefront/tastehouse";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const s=await prisma.store.findUnique({where:{slug},include:{template:true,business:true,reviews:{include:{author:true},orderBy:{createdAt:"desc"},take:30}}}); if(!s||s.status!=="ACTIVE"||!s.template?.name?.startsWith("TasteHouse"))notFound(); return <TasteHouseReviews store={{...s,sellsProducts:s.business?.sellsProducts??true}} slug={slug} reviews={s.reviews}/>;}