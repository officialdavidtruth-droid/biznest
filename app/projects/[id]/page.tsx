import{notFound}from"next/navigation";import{getCreativeProjectPublic}from"@/lib/actions/creative-projects";import{ProjectReviewActions}from"@/components/projects/project-review-actions";export default async function ProjectReviewPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{token?:string}>}){const{id}=await params;const{token=""}=await searchParams;const p=await getCreativeProjectPublic(id,token);if(!p)notFound();const latest=p.revisions[0];return <div className="min-h-screen bg-slate-50 px-4 py-10"><div className="mx-auto max-w-3xl overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-slate-200"><div className="border-b p-6"><p className="text-xs font-bold uppercase tracking-widest text-slate-500">{p.store.name}</p><h1 className="mt-2 text-2xl font-bold">Project {p.projectNo}</h1><p className="mt-1 text-sm text-slate-500">{p.serviceType} · {p.status.replaceAll("_"," ")}</p></div><div className="p-6"><div className="rounded-2xl bg-slate-50 p-5"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Project brief</p><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-slate-700">{p.brief}</p></div>{latest&&<div className="mt-6"><p className="mb-3 text-sm font-semibold">Latest design · Version {latest.version}</p><img src={latest.previewUrl} alt={`Project ${p.projectNo} version ${latest.version}`} className="w-full rounded-2xl border object-contain"/><p className="mt-3 text-sm text-slate-500">{latest.note}</p></div>}<ProjectReviewActions projectId={p.id} token={token} status={p.status}/></div></div></div>}
