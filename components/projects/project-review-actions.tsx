"use client";import{useState}from"react";import{toast}from"sonner";import{approveCreativeProject,requestCreativeChanges}from"@/lib/actions/creative-projects";export function ProjectReviewActions({projectId,token,status}:{projectId:string;token:string;status:string}){const[note,setNote]=useState("");const[busy,setBusy]=useState(false);if(status!=="AWAITING_APPROVAL")return <div className="mt-6 rounded-xl border bg-slate-50 p-4 text-sm text-slate-600">Current project status: <strong>{status.replaceAll("_"," ")}</strong></div>;return <div className="mt-6 space-y-3"><textarea value={note} onChange={e=>setNote(e.target.value)} className="w-full rounded-xl border p-3 text-sm" placeholder="Optional approval note, or describe changes you need…"/><div className="grid gap-3 sm:grid-cols-2"><button disabled={busy} onClick={async()=>{setBusy(true);const r=await approveCreativeProject(projectId,token,note);setBusy(false);r.success?toast.success("Design approved"):toast.error(r.error)}} className="rounded-xl bg-emerald-600 px-4 py-3 text-sm font-semibold text-white disabled:opacity-60">Approve design</button><button disabled={busy} onClick={async()=>{setBusy(true);const r=await requestCreativeChanges(projectId,token,note);setBusy(false);r.success?toast.success("Changes sent to the business"):toast.error(r.error)}} className="rounded-xl border px-4 py-3 text-sm font-semibold disabled:opacity-60">Request changes</button></div></div>}
