"use client";

import Link from "next/link";
import { useMemo, useState, useTransition } from "react";
import {
  AlertTriangle, ArrowLeft, ArrowRight, BarChart3, Boxes, CalendarDays, ChefHat,
  ClipboardCheck, Clock3, DollarSign, FileSignature, LayoutDashboard, PackageCheck,
  Plus, RefreshCw, Search, Settings2, ShoppingBag, Store, Trash2, Truck, Users,
  UtensilsCrossed, WalletCards, XCircle, Zap,
} from "lucide-react";
import { toast } from "sonner";
import { saveFnbRecipe, recordFnbWaste, setFnbRotationMode, updateFnbKitchenOrderStatus } from "@/lib/actions/fnb";
import { extractFnbRecipe } from "@/lib/fnb-utils";
import type { FnbRotationMode } from "@/lib/fnb-settings";

function money(n: number) { return `₦${Number(n || 0).toLocaleString()}`; }
function date(v: string | Date) { return new Date(v).toLocaleDateString(undefined, { day: "2-digit", month: "short" }); }
function time(v: string | Date) { return new Date(v).toLocaleTimeString(undefined, { hour: "2-digit", minute: "2-digit" }); }
function label(v: string) { return v.replaceAll("_", " ").toLowerCase().replace(/(^| )\w/g, (m) => m.toUpperCase()); }
function badge(v: string) {
  if (["PAID", "COMPLETED", "DELIVERED", "AVAILABLE", "IN_STOCK", "RECEIVED"].includes(v)) return "bg-emerald-500/10 text-emerald-700 border-emerald-500/20";
  if (["IN_PROGRESS", "CONFIRMED", "SENT", "PARTIALLY_RECEIVED"].includes(v)) return "bg-sky-500/10 text-sky-700 border-sky-500/20";
  if (["CANCELLED", "REFUNDED", "OUT_OF_STOCK"].includes(v)) return "bg-rose-500/10 text-rose-700 border-rose-500/20";
  return "bg-amber-500/10 text-amber-700 border-amber-500/20";
}

type Tab = "dashboard" | "pos" | "kitchen" | "menu" | "recipes" | "tables" | "inventory" | "procurement" | "suppliers" | "customers" | "wastage" | "reports" | "settings";

const tabs: Array<{ id: Tab; label: string; icon: any }> = [
  { id: "dashboard", label: "Overview", icon: LayoutDashboard },
  { id: "pos", label: "POS & Sales", icon: ShoppingBag },
  { id: "kitchen", label: "Kitchen", icon: ChefHat },
  { id: "menu", label: "Menu", icon: UtensilsCrossed },
  { id: "recipes", label: "Recipes & Food Cost", icon: ClipboardCheck },
  { id: "tables", label: "Tables & Reservations", icon: CalendarDays },
  { id: "inventory", label: "Inventory", icon: Boxes },
  { id: "procurement", label: "Procurement", icon: FileSignature },
  { id: "suppliers", label: "Suppliers", icon: Truck },
  { id: "customers", label: "Customers", icon: Users },
  { id: "wastage", label: "Wastage", icon: Trash2 },
  { id: "reports", label: "Reports", icon: BarChart3 },
  { id: "settings", label: "Settings", icon: Settings2 },
];

export function FnbWorkspace({
  slug,
  data,
  reservations = [],
  units = [],
}: {
  slug: string;
  data: any;
  reservations?: Array<{
    id: string;
    scheduledAt: string;
    status: string;
    partySize: number | null;
    specialRequests: string[] | string | null;
    guestName: string;
    guestPhone: string;
    unitId: string | null;
    unitLabel: string | null;
  }>;
  units?: Array<{ id: string; label: string; location: string | null; capacity: number | null }>;
}) {
  const [tab, setTab] = useState<Tab>("dashboard");
  const [mobileOpen, setMobileOpen] = useState(false);
  const m = data.metrics;
  const reservationCount = reservations.length || Number(data.reservations || 0);
  const navigate = (next: Tab) => { setTab(next); setMobileOpen(false); };

  return <div className="min-h-screen bg-background text-foreground">
    <header className="sticky top-0 z-30 border-b bg-background/95 backdrop-blur">
      <div className="flex items-center gap-3 px-4 py-3 lg:px-7">
        <button className="lg:hidden rounded-lg border p-2" onClick={() => setMobileOpen(!mobileOpen)} aria-label="Open FnB menu"><LayoutDashboard className="h-4 w-4" /></button>
        <Link href={`/store/${slug}/admin/apps`} className="hidden items-center gap-2 text-xs font-semibold text-muted-foreground hover:text-foreground lg:flex"><ArrowLeft className="h-4 w-4" /> Apps</Link>
        <div className="h-5 w-px bg-border hidden lg:block" />
        <div className="min-w-0 flex-1"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-primary">BizNest FnB</p><p className="truncate text-sm font-bold">{data.storeName} · Restaurant Operations</p></div>
        <Link href={`/store/${slug}/admin/pos`} className="hidden rounded-xl bg-primary px-4 py-2 text-xs font-bold text-primary-foreground sm:inline-flex">Open POS</Link>
      </div>
    </header>
    <div className="flex">
      {mobileOpen && <button className="fixed inset-0 z-30 bg-black/20 lg:hidden" onClick={() => setMobileOpen(false)} aria-label="Close menu" />}
      <aside className={`${mobileOpen ? "translate-x-0" : "-translate-x-full"} fixed inset-y-0 left-0 z-40 w-72 border-r bg-background pt-16 transition-transform lg:sticky lg:top-[57px] lg:h-[calc(100vh-57px)] lg:w-64 lg:translate-x-0 lg:self-start lg:pt-0`}>
        <nav className="h-full overflow-y-auto p-3">
          {tabs.map(({ id, label: text, icon: Icon }) => <button key={id} onClick={() => navigate(id)} className={`mb-1 flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-xs font-semibold ${tab === id ? "bg-primary/10 text-primary" : "text-muted-foreground hover:bg-muted hover:text-foreground"}`}><Icon className="h-4 w-4" />{text}</button>)}
          <div className="mt-4 border-t pt-4 text-[10px] text-muted-foreground"><p className="px-3 font-semibold uppercase tracking-wider">Core BizNest</p><Link href={`/store/${slug}/admin/inventory`} className="mt-2 flex items-center gap-2 px-3 py-2 hover:text-foreground"><Boxes className="h-3.5 w-3.5" /> Inventory</Link><Link href={`/store/${slug}/admin/purchase-orders`} className="flex items-center gap-2 px-3 py-2 hover:text-foreground"><FileSignature className="h-3.5 w-3.5" /> Purchase orders</Link></div>
        </nav>
      </aside>
      <main className="min-w-0 flex-1 px-4 py-5 lg:px-7 lg:py-7">
        {tab === "dashboard" && <Dashboard data={{ ...data, reservations: reservationCount, reservationRows: reservations, reservationUnits: units }} navigate={navigate} />}
        {tab === "pos" && <LinkPanel title="POS & Sales" description="Run walk-in and in-person sales through the existing hardened BizNest register." href={`/store/${slug}/admin/pos`} label="Open POS Register" icon={ShoppingBag} />}
        {tab === "kitchen" && <Kitchen slug={slug} orders={data.orders} />}
        {tab === "menu" && <MenuPanel slug={slug} products={data.products} />}
        {tab === "recipes" && <Recipes slug={slug} products={data.products} inventory={data.inventory} />}
        {tab === "tables" && <LinkPanel title="Tables & Reservations" description="Use BizNest bookings for reservations and the restaurant table workflow. This keeps reservation records in the same core booking system." href={`/store/${slug}/admin/bookings`} label="Open Reservations" icon={CalendarDays} />}
        {tab === "inventory" && <InventoryPanel slug={slug} data={data} />}
        {tab === "procurement" && <ProcurementPanel slug={slug} purchaseOrders={data.purchaseOrders} />}
        {tab === "suppliers" && <SupplierPanel slug={slug} suppliers={data.suppliers} />}
        {tab === "customers" && <CustomerPanel slug={slug} customers={data.customers} />}
        {tab === "wastage" && <WastagePanel slug={slug} data={data} />}
        {tab === "reports" && <Reports data={data} />}
        {tab === "settings" && <SettingsPanel slug={slug} mode={data.mode} />}
      </main>
    </div>
  </div>;
}

function Dashboard({ data, navigate }: { data: any; navigate: (t: Tab) => void }) {
  const m = data.metrics;
  const alerts = m.outOfStock + m.lowStock + m.expired + m.expiringSoon;
  return <div className="space-y-6">
    <div><p className="text-xs font-bold uppercase tracking-wider text-primary">Food & Beverage Control Center</p><h1 className="mt-1 text-2xl font-bold tracking-tight">{data.storeName} operations</h1><p className="mt-1 max-w-3xl text-sm text-muted-foreground">One operating center for service, stock, kitchen, menu costing, procurement, customers and restaurant performance.</p></div>
    <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">
      <Metric icon={DollarSign} label="Today's sales" value={money(m.revenue)} sub={`${data.orders.length} order records today`} />
      <Metric icon={ShoppingBag} label="Today's orders" value={data.orders.length} sub={`${m.activeOrders} active`} />
      <Metric icon={CalendarDays} label="Reservations" value={data.reservations} sub="Today" />
      <Metric icon={Boxes} label="Stock value" value={money(m.stockValue)} sub="Known cost basis" />
      <Metric icon={AlertTriangle} label="Low stock" value={m.lowStock} sub={`${m.outOfStock} out of stock`} />
      <Metric icon={Clock3} label="Expiring soon" value={m.expiringSoon} sub={`${m.expired} expired batches`} />
      <Metric icon={ClipboardCheck} label="Recipes" value={m.recipeCount} sub="Menu items with recipes" />
      <Metric icon={Trash2} label="Waste units" value={m.wasteUnits} sub="Recent recorded waste" />
    </div>
    {alerts > 0 && <div className="flex flex-col gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-amber-900 sm:flex-row sm:items-center"><AlertTriangle className="h-5 w-5" /><div className="flex-1"><p className="text-sm font-bold">Operations need attention</p><p className="mt-1 text-xs">{m.lowStock} low-stock, {m.outOfStock} out-of-stock, {m.expiringSoon} expiring and {m.expired} expired batches are currently visible.</p></div><button onClick={() => navigate("inventory")} className="text-xs font-bold underline">Review inventory</button></div>}
    <div className="grid gap-4 xl:grid-cols-3">
      <QuickCard title="Service" icon={ChefHat} items={["POS & Sales", "Kitchen", "Tables & Reservations"]} onClick={(i) => navigate(i === 0 ? "pos" : i === 1 ? "kitchen" : "tables")} />
      <QuickCard title="Stock & Purchasing" icon={Boxes} items={["Inventory", "Recipes & Food Cost", "Procurement"]} onClick={(i) => navigate(i === 0 ? "inventory" : i === 1 ? "recipes" : "procurement")} />
      <QuickCard title="Growth & Control" icon={BarChart3} items={["Customers", "Wastage", "Reports"]} onClick={(i) => navigate(i === 0 ? "customers" : i === 1 ? "wastage" : "reports")} />
    </div>
    <section className="overflow-hidden rounded-2xl border bg-background shadow-sm"><div className="flex items-center justify-between border-b px-5 py-4"><div><h2 className="text-sm font-bold">Today's activity</h2><p className="mt-1 text-xs text-muted-foreground">Latest online and POS orders.</p></div><button onClick={() => navigate("pos")} className="text-xs font-bold text-primary">Open register <ArrowRight className="ml-1 inline h-3.5 w-3.5" /></button></div><div className="divide-y">{data.orders.slice(0, 8).map((o: any) => <div key={o.id} className="flex items-center gap-4 px-5 py-3.5"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-muted"><ShoppingBag className="h-4 w-4" /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{o.posCustomerName || (o.channel === "POS" ? "Walk-in customer" : `Order ${o.id.slice(-6).toUpperCase()}`)}</p><p className="mt-0.5 text-[10px] text-muted-foreground">{o.channel} · {o.items.reduce((s: number, i: any) => s + i.quantity, 0)} item(s) · {time(o.createdAt)}</p></div><span className={`rounded-full border px-2 py-1 text-[10px] font-bold ${badge(o.status)}`}>{label(o.status)}</span><p className="text-sm font-bold">{o.currency} {Number(o.total).toLocaleString()}</p></div>)}{data.orders.length === 0 && <Empty text="No orders recorded today." />}</div></section>
  </div>;
}

function Kitchen({ slug, orders }: { slug: string; orders: any[] }) {
  const [busy, start] = useTransition();
  const kitchenOrders = orders.filter((o) => ["PAID", "IN_PROGRESS", "DELIVERED"].includes(o.status));
  const advance = (o: any) => {
    const next = o.status === "PAID" ? "IN_PROGRESS" : o.status === "IN_PROGRESS" ? "DELIVERED" : "COMPLETED";
    start(async () => { const r = await updateFnbKitchenOrderStatus(slug, o.id, next as any); if (!r.success) toast.error(r.error); else toast.success(`Order moved to ${label(next)}`); });
  };
  return <div className="space-y-5"><SectionTitle title="Kitchen Display" description="A live kitchen queue using the same order records as POS and online checkout." action={<Link href={`/store/${slug}/admin/orders`} className="rounded-xl border px-3 py-2 text-xs font-bold">All orders</Link>} /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{kitchenOrders.map((o: any) => <div key={o.id} className="rounded-2xl border bg-background p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-bold">#{o.id.slice(-6).toUpperCase()}</p><p className="mt-1 text-[10px] text-muted-foreground">{o.channel} · {time(o.createdAt)}</p></div><span className={`rounded-full border px-2 py-1 text-[10px] font-bold ${badge(o.status)}`}>{label(o.status)}</span></div><div className="mt-4 space-y-2">{o.items.map((i: any, idx: number) => <div key={idx} className="flex justify-between rounded-lg bg-muted/40 px-3 py-2 text-xs"><span>{i.quantity} × {i.variant?.product?.name ? `${i.variant.product.name} — ${i.variant.label}` : i.product?.name || "Menu item"}</span></div>)}</div><button disabled={busy} onClick={() => advance(o)} className="mt-4 w-full rounded-xl bg-primary px-3 py-2.5 text-xs font-bold text-primary-foreground disabled:opacity-50">{o.status === "PAID" ? "Start preparing" : o.status === "IN_PROGRESS" ? "Mark ready" : "Complete order"}</button></div>)}{kitchenOrders.length === 0 && <Empty text="No active kitchen tickets right now." />}</div></div>;
}

function MenuPanel({ slug, products }: { slug: string; products: any[] }) { return <div className="space-y-5"><SectionTitle title="Menu Management" description="Manage the sellable catalog while keeping the F&B workspace focused on service." action={<Link href={`/store/${slug}/admin/products`} className="rounded-xl bg-primary px-3 py-2 text-xs font-bold text-primary-foreground">Manage catalog</Link>} /><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{products.slice(0, 30).map((p: any) => <div key={p.id} className="rounded-2xl border p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-bold">{p.name}</p><p className="mt-1 text-xs text-muted-foreground">{money(p.price)} · {p.inventory ? `${p.inventory.quantity} in stock` : "No stock tracking"}</p></div><span className={`rounded-full border px-2 py-1 text-[10px] font-bold ${p.inventory && p.inventory.quantity === 0 ? badge("OUT_OF_STOCK") : badge("AVAILABLE")}`}>{p.inventory && p.inventory.quantity === 0 ? "Unavailable" : p.isPublished ? "Published" : "Hidden"}</span></div><p className="mt-3 text-[10px] text-muted-foreground">{extractFnbRecipe(p.attributes) ? "Recipe configured" : "No recipe configured"}</p></div>)}</div></div>; }

function Recipes({ slug, products, inventory }: { slug: string; products: any[]; inventory: any[] }) {
  const [productId, setProductId] = useState(products[0]?.id || "");
  const product = products.find((p) => p.id === productId) || products[0];
  const existing = product ? extractFnbRecipe(product.attributes) : null;
  const [ingredients, setIngredients] = useState<any[]>(existing?.ingredients || []);
  const [yieldQty, setYieldQty] = useState(existing?.yieldQty || 1);
  const [busy, start] = useTransition();
  const selectProduct = (id: string) => { const p = products.find((x) => x.id === id); setProductId(id); const r = p ? extractFnbRecipe(p.attributes) : null; setIngredients(r?.ingredients || []); setYieldQty(r?.yieldQty || 1); };
  const addIngredient = () => { const first = inventory.find((i) => !ingredients.some((x) => x.inventoryItemId === i.id)); if (!first) return; setIngredients([...ingredients, { inventoryItemId: first.id, quantity: 1, unit: "unit", name: first.product.name }]); };
  const save = () => { if (!product) return; start(async () => { const r = await saveFnbRecipe(slug, product.id, { yieldQty: Number(yieldQty), ingredients: ingredients.map((i) => ({ inventoryItemId: i.inventoryItemId, quantity: Number(i.quantity), unit: i.unit })) }); if (!r.success) toast.error(r.error); else toast.success("Recipe saved"); }); };
  return <div className="space-y-5"><SectionTitle title="Recipes & Food Cost" description="Attach ingredient recipes to menu items so food cost can be calculated from real stock costs." action={<button onClick={addIngredient} className="rounded-xl border px-3 py-2 text-xs font-bold"><Plus className="mr-1 inline h-3.5 w-3.5" /> Ingredient</button>} /><div className="grid gap-5 xl:grid-cols-[320px_1fr]"><div className="rounded-2xl border p-4"><label className="text-xs font-bold">Menu item</label><select value={productId} onChange={(e) => selectProduct(e.target.value)} className="mt-2 w-full rounded-xl border bg-background px-3 py-2 text-sm">{products.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}</select><label className="mt-4 block text-xs font-bold">Yield / portions</label><input type="number" min="1" value={yieldQty} onChange={(e) => setYieldQty(Number(e.target.value))} className="mt-2 w-full rounded-xl border bg-background px-3 py-2 text-sm" /><div className="mt-5 rounded-xl bg-muted/50 p-3 text-xs"><p className="font-bold">Why this matters</p><p className="mt-1 text-muted-foreground">Recipes turn menu sales into ingredient-level costing. The current POS remains the sales source of truth while recipes provide the operational cost basis.</p></div></div><div className="rounded-2xl border p-4"><div className="mb-3 grid grid-cols-[1fr_110px_110px_32px] gap-2 text-[10px] font-bold uppercase tracking-wide text-muted-foreground"><span>Ingredient</span><span>Qty</span><span>Unit</span><span /></div>{ingredients.map((i, idx) => <div key={`${i.inventoryItemId}-${idx}`} className="mb-2 grid grid-cols-[1fr_110px_110px_32px] gap-2"><select value={i.inventoryItemId} onChange={(e) => setIngredients(ingredients.map((x, n) => n === idx ? { ...x, inventoryItemId: e.target.value, name: inventory.find((v) => v.id === e.target.value)?.product.name || x.name } : x))} className="rounded-lg border bg-background px-2 py-2 text-xs">{inventory.map((v) => <option key={v.id} value={v.id}>{v.product.name}</option>)}</select><input type="number" min="0.001" step="0.001" value={i.quantity} onChange={(e) => setIngredients(ingredients.map((x, n) => n === idx ? { ...x, quantity: Number(e.target.value) } : x))} className="rounded-lg border bg-background px-2 py-2 text-xs" /><input value={i.unit} onChange={(e) => setIngredients(ingredients.map((x, n) => n === idx ? { ...x, unit: e.target.value } : x))} className="rounded-lg border bg-background px-2 py-2 text-xs" /><button onClick={() => setIngredients(ingredients.filter((_, n) => n !== idx))} className="rounded-lg border text-muted-foreground hover:text-destructive"><Trash2 className="mx-auto h-3.5 w-3.5" /></button></div>)}{ingredients.length === 0 && <Empty text="No ingredients yet. Add the ingredients required for one recipe yield." />}<div className="mt-4 flex justify-end"><button disabled={busy || !product} onClick={save} className="rounded-xl bg-primary px-4 py-2.5 text-xs font-bold text-primary-foreground disabled:opacity-50">Save recipe</button></div></div></div></div>;
}

function InventoryPanel({ slug, data }: { slug: string; data: any }) { const [q, setQ] = useState(""); const rows = data.inventory.filter((i: any) => i.product.name.toLowerCase().includes(q.toLowerCase())); return <div className="space-y-5"><SectionTitle title="Inventory & Stock Rotation" description="Track stock, batches, expiry, cost and the FIFO/FEFO control policy." action={<Link href={`/store/${slug}/admin/inventory`} className="rounded-xl bg-primary px-3 py-2 text-xs font-bold text-primary-foreground">Open full inventory</Link>} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={Boxes} label="Tracked items" value={data.inventory.length} sub="Inventory records" /><Metric icon={AlertTriangle} label="Low stock" value={data.metrics.lowStock} sub="At/below threshold" /><Metric icon={XCircle} label="Out of stock" value={data.metrics.outOfStock} sub="Unavailable" /><Metric icon={PackageCheck} label={`${data.mode} value`} value={money(data.metrics.batchValue)} sub="Active batch cost" /></div><div className="rounded-2xl border p-4"><div className="relative mb-3 max-w-md"><Search className="absolute left-3 top-2.5 h-4 w-4 text-muted-foreground" /><input value={q} onChange={(e) => setQ(e.target.value)} placeholder="Search stock..." className="w-full rounded-xl border bg-background py-2 pl-9 pr-3 text-xs" /></div><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-xs"><thead className="border-b text-[10px] uppercase tracking-wide text-muted-foreground"><tr><th className="pb-2">Item</th><th className="pb-2">On hand</th><th className="pb-2">Cost</th><th className="pb-2">Status</th><th className="pb-2">Lots</th></tr></thead><tbody>{rows.map((i: any) => { const lots = data.batches.filter((b: any) => b.inventoryItem?.product?.name === i.product.name); const st = i.quantity === 0 ? "OUT_OF_STOCK" : i.quantity <= i.lowStockThreshold ? "LOW_STOCK" : "IN_STOCK"; return <tr key={i.id} className="border-b last:border-0"><td className="py-3 font-semibold">{i.product.name}</td><td>{i.quantity}</td><td>{i.costPrice == null ? "—" : money(i.costPrice)}</td><td><span className={`rounded-full border px-2 py-1 text-[10px] font-bold ${badge(st)}`}>{label(st)}</span></td><td>{lots.length}</td></tr>; })}</tbody></table></div></div><section className="rounded-2xl border p-4"><div className="flex items-center gap-2"><PackageCheck className="h-4 w-4 text-primary" /><h2 className="text-sm font-bold">Batch rotation</h2></div><p className="mt-1 text-xs text-muted-foreground">FIFO consumes the oldest received lot first. FEFO consumes the nearest usable expiry first. Both remain auditable through batch consumption records.</p><div className="mt-4 grid gap-2 md:grid-cols-2">{data.batches.slice(0, 12).map((b: any) => <div key={b.id} className="rounded-xl border p-3"><div className="flex justify-between gap-3"><p className="text-xs font-semibold">{b.inventoryItem?.product?.name || `${b.variant?.product?.name || "Item"} · ${b.variant?.label || ""}`}</p><span className="text-xs font-bold">{b.quantityRemaining}</span></div><p className="mt-1 text-[10px] text-muted-foreground">{b.batchNumber || "No batch number"} · received {date(b.receivedAt)}{b.expiryDate ? ` · expires ${date(b.expiryDate)}` : ""}</p></div>)}</div></section></div>; }

function ProcurementPanel({ slug, purchaseOrders }: { slug: string; purchaseOrders: any[] }) { return <div className="space-y-5"><SectionTitle title="Procurement" description="Move low-stock demand into controlled purchase requests and purchase orders." action={<Link href={`/store/${slug}/admin/purchase-orders/new`} className="rounded-xl bg-primary px-3 py-2 text-xs font-bold text-primary-foreground">New purchase order</Link>} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={FileSignature} label="Recent POs" value={purchaseOrders.length} sub="Latest records" /><Metric icon={Clock3} label="Awaiting receipt" value={purchaseOrders.filter((p) => ["SENT", "PARTIALLY_RECEIVED"].includes(p.status)).length} sub="Open procurement" /><Metric icon={Truck} label="Suppliers" value={new Set(purchaseOrders.map((p) => p.supplier.name)).size} sub="Used in recent POs" /><Metric icon={DollarSign} label="PO value" value={money(purchaseOrders.reduce((s, p) => s + Number(p.subtotal), 0))} sub="Recent PO subtotal" /></div><div className="overflow-hidden rounded-2xl border"><table className="w-full text-left text-xs"><thead className="border-b bg-muted/30 text-[10px] uppercase tracking-wide text-muted-foreground"><tr><th className="px-4 py-3">PO</th><th>Supplier</th><th>Status</th><th>Value</th></tr></thead><tbody>{purchaseOrders.map((p) => <tr key={p.id} className="border-b last:border-0"><td className="px-4 py-3 font-semibold">{p.poNumber}</td><td>{p.supplier.name}</td><td><span className={`rounded-full border px-2 py-1 text-[10px] font-bold ${badge(p.status)}`}>{label(p.status)}</span></td><td>{p.currency} {Number(p.subtotal).toLocaleString()}</td></tr>)}</tbody></table>{purchaseOrders.length === 0 && <Empty text="No purchase orders yet." />}</div></div>; }

function SupplierPanel({ slug, suppliers }: { slug: string; suppliers: any[] }) { return <div className="space-y-5"><SectionTitle title="Suppliers" description="Your supplier directory is shared with BizNest procurement so vendor records stay in one place." action={<Link href={`/store/${slug}/admin/suppliers`} className="rounded-xl border px-3 py-2 text-xs font-bold">Manage suppliers</Link>} /><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{suppliers.map((s) => <div key={s.id} className="rounded-2xl border p-4"><p className="text-sm font-bold">{s.name}</p><p className="mt-1 text-xs text-muted-foreground">{s.contactName || "No contact name"}</p><p className="mt-3 text-xs">{s.phone || s.email || "No contact details"}</p></div>)}{suppliers.length === 0 && <Empty text="No suppliers yet." />}</div></div>; }

function CustomerPanel({ slug, customers }: { slug: string; customers: any[] }) { return <div className="space-y-5"><SectionTitle title="Customers" description="Keep guest and buyer information connected to order history and CRM." action={<Link href={`/store/${slug}/admin/customers`} className="rounded-xl border px-3 py-2 text-xs font-bold">Open customer list</Link>} /><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{customers.map((c) => <div key={c.id} className="rounded-2xl border p-4"><p className="text-sm font-bold">{c.name}</p><p className="mt-1 text-xs text-muted-foreground">{c.phone || c.email || "No contact"}</p><p className="mt-3 text-[10px] text-muted-foreground">Updated {date(c.updatedAt)}</p></div>)}{customers.length === 0 && <Empty text="No customer profiles yet." />}</div></div>; }

function WastagePanel({ slug, data }: { slug: string; data: any }) { const [item, setItem] = useState(data.inventory[0]?.id || ""); const [qty, setQty] = useState(1); const [reason, setReason] = useState(""); const [busy, start] = useTransition(); const submit = () => start(async () => { const r = await recordFnbWaste(slug, item, Number(qty), reason); if (!r.success) toast.error(r.error); else { toast.success("Waste recorded and stock reconciled"); setReason(""); setQty(1); } }); return <div className="space-y-5"><SectionTitle title="Wastage Control" description="Record spoilage, expired stock, damaged food and overproduction with an auditable stock movement." /><div className="grid gap-5 xl:grid-cols-[420px_1fr]"><div className="rounded-2xl border p-5"><label className="text-xs font-bold">Stock item</label><select value={item} onChange={(e) => setItem(e.target.value)} className="mt-2 w-full rounded-xl border bg-background px-3 py-2.5 text-sm">{data.inventory.map((i: any) => <option key={i.id} value={i.id}>{i.product.name} · {i.quantity} available</option>)}</select><label className="mt-4 block text-xs font-bold">Quantity wasted</label><input type="number" min="1" value={qty} onChange={(e) => setQty(Number(e.target.value))} className="mt-2 w-full rounded-xl border bg-background px-3 py-2.5 text-sm" /><label className="mt-4 block text-xs font-bold">Reason</label><textarea value={reason} onChange={(e) => setReason(e.target.value)} placeholder="Expired, spoiled, burnt, damaged, overproduction..." className="mt-2 min-h-24 w-full rounded-xl border bg-background px-3 py-2.5 text-sm" /><button disabled={busy} onClick={submit} className="mt-4 w-full rounded-xl bg-primary px-4 py-2.5 text-xs font-bold text-primary-foreground disabled:opacity-50">Record waste</button></div><div className="rounded-2xl border p-5"><h2 className="text-sm font-bold">Recent waste</h2><div className="mt-4 divide-y">{data.wasteMovements.map((w: any) => <div key={w.id} className="flex items-center gap-3 py-3"><Trash2 className="h-4 w-4 text-rose-600" /><div className="min-w-0 flex-1"><p className="text-xs font-semibold">{w.inventoryItem?.product?.name || "Stock item"}</p><p className="mt-0.5 text-[10px] text-muted-foreground">{String(w.note || "").replace("FNB WASTE: ", "")} · {date(w.createdAt)}</p></div><span className="text-xs font-bold">{Math.abs(w.quantityChange)}</span></div>)}{data.wasteMovements.length === 0 && <Empty text="No waste records yet." />}</div></div></div></div>; }

function Reports({ data }: { data: any }) { const m = data.metrics; const foodCostProxy = data.products.reduce((sum: number, p: any) => { const r = extractFnbRecipe(p.attributes); if (!r) return sum; return sum + r.ingredients.reduce((s: number, i: any) => { const stock = data.inventory.find((x: any) => x.id === i.inventoryItemId); return s + (stock?.costPrice || 0) * i.quantity; }, 0); }, 0); return <div className="space-y-5"><SectionTitle title="F&B Reports" description="Operational KPIs from the live BizNest order, inventory, batch and procurement records." /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={DollarSign} label="Today's sales" value={money(m.revenue)} sub="Orders today" /><Metric icon={Boxes} label="Inventory value" value={money(m.stockValue)} sub="Known cost basis" /><Metric icon={Zap} label="Recipe ingredient cost" value={money(foodCostProxy)} sub="Configured recipe inputs" /><Metric icon={Trash2} label="Waste units" value={m.wasteUnits} sub="Recent recorded waste" /></div><div className="grid gap-4 lg:grid-cols-2"><section className="rounded-2xl border p-5"><h2 className="text-sm font-bold">Top operational risks</h2><div className="mt-4 space-y-3"><ReportRow label="Out of stock" value={m.outOfStock} /><ReportRow label="Low stock" value={m.lowStock} /><ReportRow label="Expiring within 7 days" value={m.expiringSoon} /><ReportRow label="Expired batches" value={m.expired} /></div></section><section className="rounded-2xl border p-5"><h2 className="text-sm font-bold">Business flow</h2><div className="mt-4 space-y-3"><ReportRow label="Orders today" value={data.orders.length} /><ReportRow label="Active kitchen orders" value={m.activeOrders} /><ReportRow label="Reservations today" value={data.reservations} /><ReportRow label="Menu recipes configured" value={m.recipeCount} /></div></section></div></div>; }

function SettingsPanel({ slug, mode }: { slug: string; mode: FnbRotationMode }) { const [value, setValue] = useState(mode); const [busy, start] = useTransition(); const save = () => start(async () => { const r = await setFnbRotationMode(slug, value); if (!r.success) toast.error(r.error); else toast.success(`Stock rotation set to ${value}`); }); return <div className="space-y-5"><SectionTitle title="FnB Settings" description="Configure how BizNest allocates stock from tracked batches." /><section className="max-w-3xl rounded-2xl border p-5"><div className="flex items-start gap-3"><PackageCheck className="mt-0.5 h-5 w-5 text-primary" /><div><h2 className="text-sm font-bold">Stock rotation policy</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">Choose the policy used when stock is consumed. FIFO prioritizes the oldest received batch. FEFO prioritizes the nearest usable expiry. Every consumption remains linked to the stock movement ledger.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2"><button onClick={() => setValue("FIFO")} className={`rounded-xl border p-4 text-left ${value === "FIFO" ? "border-primary bg-primary/5" : ""}`}><p className="text-sm font-bold">FIFO — First In, First Out</p><p className="mt-1 text-xs text-muted-foreground">Oldest received stock is consumed first.</p></button><button onClick={() => setValue("FEFO")} className={`rounded-xl border p-4 text-left ${value === "FEFO" ? "border-primary bg-primary/5" : ""}`}><p className="text-sm font-bold">FEFO — First Expired, First Out</p><p className="mt-1 text-xs text-muted-foreground">Nearest usable expiry is consumed first.</p></button></div><button disabled={busy} onClick={save} className="mt-4 rounded-xl bg-primary px-4 py-2.5 text-xs font-bold text-primary-foreground disabled:opacity-50">Save rotation policy</button></section><section className="max-w-3xl rounded-2xl border p-5"><h2 className="text-sm font-bold">Operational architecture</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">The FnB workspace is a dedicated vertical route, like BizNest PMS. The heavy operations UI is not nested inside the marketplace shell, reducing layout conflicts and making navigation safer.</p></section></div>; }

function LinkPanel({ title, description, href, label: actionLabel, icon: Icon }: any) { return <div className="mx-auto flex min-h-[55vh] max-w-3xl items-center"><section className="w-full rounded-3xl border bg-background p-7 text-center shadow-sm"><span className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-primary/10 text-primary"><Icon className="h-7 w-7" /></span><h1 className="mt-5 text-xl font-bold">{title}</h1><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-muted-foreground">{description}</p><Link href={href} className="mt-6 inline-flex items-center gap-2 rounded-xl bg-primary px-5 py-3 text-xs font-bold text-primary-foreground">{actionLabel}<ArrowRight className="h-4 w-4" /></Link></section></div>; }
function SectionTitle({ title, description, action }: any) { return <div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-xl font-bold">{title}</h1><p className="mt-1 max-w-3xl text-sm text-muted-foreground">{description}</p></div>{action}</div>; }
function Metric({ icon: Icon, label: text, value, sub }: any) { return <div className="rounded-2xl border bg-background p-4 shadow-sm"><div className="flex items-center justify-between"><span className="text-[11px] font-medium text-muted-foreground">{text}</span><Icon className="h-4 w-4 text-primary" /></div><p className="mt-2 text-xl font-bold tracking-tight">{value}</p><p className="mt-1 text-[10px] text-muted-foreground">{sub}</p></div>; }
function QuickCard({ title, icon: Icon, items, onClick }: { title: string; icon: any; items: string[]; onClick: (i: number) => void }) { return <section className="rounded-2xl border p-5"><div className="flex items-center gap-2"><Icon className="h-4 w-4 text-primary" /><h2 className="text-sm font-bold">{title}</h2></div><div className="mt-4 space-y-2">{items.map((x: string, i: number) => <button key={x} onClick={() => onClick(i)} className="flex w-full items-center justify-between rounded-xl border px-3 py-2.5 text-left text-xs font-semibold hover:bg-muted"><span>{x}</span><ArrowRight className="h-3.5 w-3.5 text-muted-foreground" /></button>)}</div></section>; }
function ReportRow({ label: text, value }: { label: string; value: number }) { return <div className="flex items-center justify-between rounded-xl bg-muted/40 px-3 py-2.5 text-xs"><span className="text-muted-foreground">{text}</span><span className="font-bold">{value}</span></div>; }
function Empty({ text }: { text: string }) { return <div className="col-span-full py-10 text-center text-xs text-muted-foreground">{text}</div>; }