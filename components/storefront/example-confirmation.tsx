"use client";
import Link from "next/link"; import {CheckCircle2,Truck,Mail,Headphones,ShoppingBag} from "lucide-react";
export function ExampleConfirmation({slug,order}:{slug:string;order:any;store:any}){return <main className="ex-container ex-page"><div className="confirmation"><CheckCircle2 size={60}/><h1>Order Placed Successfully!</h1><p>Thank you for shopping with Example. Your order has been confirmed and is being processed.</p><b>Order Number: #{order.id.slice(-8).toUpperCase()}</b><small>A confirmation email has been sent to your email address.</small><Link className="btn purple" href={`/store/${slug}/orders/${order.id}/track`}>View Order Details</Link></div><div className="confirmation-grid"><section className="order-details"><h2>Order Details</h2>{order.items.map((i:any)=><div key={i.id}><span>{i.product?.name||i.service?.name}</span><b>{order.currency} {Number(i.unitPrice)*i.quantity.toLocaleString()}</b></div>)}<hr/><div><span>Subtotal</span><b>{order.currency} {Number(order.subtotal).toLocaleString()}</b></div><div className="total"><span>Total</span><b>{order.currency} {Number(order.total).toLocaleString()}</b></div></section><aside><div className="track-confirm"><Truck/><h2>Track Your Order</h2><div><span>✓ Order Placed</span><span>● Processing</span><span>○ Shipped</span><span>○ Delivered</span></div><Link className="btn purple wide" href={`/store/${slug}/orders/${order.id}/track`}>Track Order</Link></div><div className="next"><Mail/><h2>What's Next?</h2><p>✓ You'll receive an order confirmation email.</p><p>✓ We'll prepare and pack your items.</p><p>✓ You'll get shipping confirmation with tracking details.</p></div><div className="next"><Headphones/><h2>Need Help?</h2><p>Our support team is here for you.</p><Link className="btn light wide" href={`/store/${slug}/support`}>Contact Support</Link></div></aside></div><div className="continue-shop"><ShoppingBag/><div><h2>Shop More Amazing Products</h2><p>Explore new arrivals and exclusive deals.</p></div><Link className="btn purple" href={`/store/${slug}`}>Continue Shopping →</Link></div></main>}
