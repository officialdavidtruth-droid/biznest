import Link from "next/link"; import {User,MapPin,CreditCard,Package,Settings,Shield,Trash2,Pencil} from "lucide-react";
export function ExampleProfileContent({slug,session,overview}:{slug:string;session:any;overview:any}){const name=session?.user?.name||"David Truth";return <div><section className="profile-card"><div className="flex items-center justify-between"><h2>Profile Information</h2><button className="btn light"><Pencil size={13}/> Edit</button></div><div className="profile-inner"><div className="avatar"><User size={44}/></div><div><h3>{name}</h3><p>{session?.user?.email}</p><p>{session?.user?.phone||""}</p><small>Account information</small></div><div className="profile-fields"><p><b>Full Name</b> {name}</p><p><b>Email Address</b> {session?.user?.email}</p><p><b>Phone Number</b> {session?.user?.phone||"Not provided"}</p></div></div></section><div className="account-cards"><section><h2><MapPin size={16}/> Default Shipping Address</h2>{overview?.defaultAddress?<p>{overview.defaultAddress.fullName}<br/>{overview.defaultAddress.line1}<br/>{overview.defaultAddress.city}, {overview.defaultAddress.state}<br/>{overview.defaultAddress.phone}</p>:<p>No saved address yet.<br/><Link href={`/store/${slug}/account/addresses`}>Add an address →</Link></p>}</section><section><h2><CreditCard size={16}/> Payment Methods</h2><p>Payment methods are managed securely by the configured payment provider.</p><Link href={`/store/${slug}/account/wallet`} className="btn light wide">+ Add New Payment Method</Link></section><section><h2><Package size={16}/> Recent Orders</h2><p><Link href={`/store/${slug}/orders`}>View your {overview?.orderCount||0} orders →</Link></p></section><section><h2><Settings size={16}/> Account Settings</h2><p><Link href="#"><Shield size={12}/> Privacy Settings　›</Link></p><p><Link href="#"><Shield size={12}/> Security　›</Link></p><p><Link href="#"><Trash2 size={12}/> Delete Account　›</Link></p></section></div></div>}
