"use client";
import { useState } from 'react';
import { Globe2, RefreshCw, CheckCircle2, Package, Palette, ExternalLink } from 'lucide-react';

type Item={id:string;type:string;name:string;category?:string|null;imageUrl?:string|null;price?:string|null;salePrice?:string|null;currency?:string|null;url?:string|null;isActive:boolean};
type Connection={websiteUrl:string;businessName?:string|null;businessType?:string|null;logoUrl?:string|null;primaryColor?:string|null;secondaryColor?:string|null;lastScannedAt?:string|null};
export function WebsiteConnector({initial,items:initialItems}:{initial:Connection|null;items:Item[]}){
 const [url,setUrl]=useState(initial?.websiteUrl||''); const [connection,setConnection]=useState(initial); const [items,setItems]=useState(initialItems); const [loading,setLoading]=useState(false); const [message,setMessage]=useState('');
 async function scan(){setMessage('');setLoading(true);try{const r=await fetch('/api/marketing/website-scan',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Could not connect website.');setConnection(j.data);setItems((j.data.items||[]).map((x:any,i:number)=>({...x,id:`scan-${i}`,isActive:true})));setMessage(`Connected successfully. Found ${(j.data.items||[]).length} products, rooms or services.`);}catch(e){setMessage(e instanceof Error?e.message:'Website scan failed.')}finally{setLoading(false)}}
 return <section className="rounded-3xl border border-emerald-100 bg-white p-6 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"><div><p className="flex items-center gap-2 text-xs font-black uppercase tracking-widest text-emerald-700"><Globe2 size={15}/> Website intelligence</p><h2 className="mt-2 text-2xl font-black">Connect your existing website</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">BizNest scans public website content to understand the business, brand and things it sells. Those items become selectable content inside email campaigns.</p></div>{connection&&<span className="inline-flex h-fit items-center gap-1.5 rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-bold text-emerald-700"><CheckCircle2 size={14}/> Connected</span>}</div><div className="mt-6 flex flex-col gap-3 sm:flex-row"><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://yourbusiness.com" className="min-w-0 flex-1 rounded-xl border border-slate-300 px-4 py-3 text-sm outline-none focus:border-emerald-600"/><button onClick={scan} disabled={loading||!url.trim()} className="inline-flex items-center justify-center gap-2 rounded-xl bg-emerald-700 px-5 py-3 text-sm font-bold text-white disabled:opacity-50">{loading?<RefreshCw className="animate-spin" size={16}/>:<Globe2 size={16}/>} {loading?'Scanning website…':connection?'Refresh website data':'Connect website'}</button></div>{message&&<p className="mt-4 rounded-xl bg-slate-50 px-4 py-3 text-sm text-slate-700">{message}</p>}{connection&&<div className="mt-6 grid gap-4 lg:grid-cols-[1fr_1.5fr]"><div className="rounded-2xl border bg-slate-50 p-5"><div className="flex items-center gap-3">{connection.logoUrl?<img src={connection.logoUrl} className="h-12 w-12 rounded-xl object-contain bg-white p-2"/>:<div className="grid h-12 w-12 place-items-center rounded-xl bg-emerald-100 text-emerald-700"><Globe2 size={20}/></div>}<div><p className="font-black">{connection.businessName||'Detected business'}</p><p className="text-xs text-slate-500">{connection.businessType||'Business'}</p></div></div><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-xl bg-white p-3"><Palette size={15} className="text-emerald-700"/><p className="mt-2 text-[11px] text-slate-500">Brand colours</p><div className="mt-2 flex gap-2"><i className="h-6 w-6 rounded-full border" style={{background:connection.primaryColor||'#0a6b3a'}}/><i className="h-6 w-6 rounded-full border" style={{background:connection.secondaryColor||'#063b25'}}/></div></div><div className="rounded-xl bg-white p-3"><Package size={15} className="text-emerald-700"/><p className="mt-2 text-[11px] text-slate-500">Marketing items</p><p className="mt-1 text-xl font-black">{items.length}</p></div></div><a href={connection.websiteUrl} target="_blank" rel="noreferrer" className="mt-4 inline-flex items-center gap-1 text-xs font-bold text-emerald-700">Open website <ExternalLink size={13}/></a></div><div className="rounded-2xl border p-5"><div className="flex items-center justify-between"><div><p className="text-sm font-black">Business catalog</p><p className="mt-1 text-xs text-slate-500">Select these in the email builder as dynamic content.</p></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold text-slate-600">{items.length} found</span></div>{items.length?<div className="mt-4 grid max-h-72 gap-2 overflow-auto">{items.slice(0,40).map(item=><div key={item.id} className="flex items-center gap-3 rounded-xl border bg-white p-3">{item.imageUrl?<img src={item.imageUrl} className="h-11 w-11 rounded-lg object-cover"/>:<div className="h-11 w-11 rounded-lg bg-slate-100"/>}<div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{item.name}</p><p className="text-xs text-slate-500">{item.type}{item.category?` · ${item.category}`:''}</p></div><div className="text-right text-xs font-bold">{item.salePrice&&<span className="mr-1 text-emerald-700">{item.salePrice}</span>}{item.price&&<span className={item.salePrice?'text-slate-400 line-through':'text-slate-700'}>{item.price}</span>}</div></div>)}</div>:<div className="mt-4 rounded-xl bg-slate-50 p-6 text-center text-sm text-slate-500">No structured products, rooms or services were detected yet. You can still use the website branding and connect supported forms later.</div>}</div></div>}</section>
}
